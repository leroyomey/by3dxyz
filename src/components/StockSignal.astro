---
import { etsyStockLabel } from "../data/products";

interface Props {
  inactive?: boolean;
}

const { inactive = false } = Astro.props;
const label = etsyStockLabel(undefined, inactive);
---

{label ? <p class:list={["stock-signal", inactive && "is-out"]}>{label}</p> : null}
